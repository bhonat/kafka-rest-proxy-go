# Compatibility fixtures

This directory holds request/response fixtures used to compare the MVP against
the Confluent REST Proxy producer API shape.

The current fixtures are project-owned golden cases. The next step is to add
captured responses from the exact Confluent REST Proxy version used in your
environment and normalize nondeterministic fields such as offsets.
